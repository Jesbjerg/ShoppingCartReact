version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:8.12.0-browsers
    working_directory: ~/shopping-cart-app
    steps:
      - run:
          name: Building the project
          command: |
            npm install
            npm build
            
      - run:
          name: Testing the project
          command: |
            npm test --single-run




