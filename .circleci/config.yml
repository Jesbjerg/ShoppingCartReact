version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:8.12.0-browsers
    steps:
      - run:
          name: Building the project
          command: |
            npm install
            npm build
            
      - run:
          name: Testing the project
          command: |
            npm test --single-run




